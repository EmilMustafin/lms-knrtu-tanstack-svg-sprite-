#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🏗️ Running pre commit checks..."
pnpm run commit:pre && pnpm run tsc:check

echo '✨🚀🏄‍♂️🍻 No error found: running commitlint now...'
